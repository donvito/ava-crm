name: contacts

frontend:
  routes:
    - /contacts

backend:
  modules:
    - contacts
  routes:
    - /api/contacts

requires:
  - database
  - companies

test_adapters:
  database: sqlite

fixtures:
  - empty_database
  - company_fixture

e2e:
  smoke:
    - tests/e2e/contacts.spec.ts
  regression:
    - tests/e2e/contacts.spec.ts
